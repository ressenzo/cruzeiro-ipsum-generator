name: Pylint

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install Pylint
        run: pip install pylint
      
      - name: Run Pylint with minimum score
        run: |
          # Run pylint and save full output
          pylint $(git ls-files '*.py') > pylint_output.txt
          
          # Extract score from output
          SCORE=$(sed -n 's/.*Your code has been rated at \([0-9.]*\).*/\1/p' pylint_output.txt)
          echo "Pylint score: $SCORE"
          
          # Fail if score < 8 and show full report again
          if (( $(echo "$SCORE < 8.0" | bc -l) )); then
            echo "❌ Pylint score below 8.0"
            echo "----- Pylint Report -----"
            cat pylint_output.txt
            exit 1
          else
            echo "✅ Pylint score OK"
          fi
  